const sansFont = weight => `
  font-family: 'Helvetica Neue', Helvetica, Arial, sans-serif;
  font-weight: ${weight};
`

const serifFont = weight => `
  font-family: 'Droid Serif', serif;
  font-weight: ${weight};
`

export { sansFont, serifFont }
